//Document Object Model(DOM)
// window Global Object
// Selection of Element of HTML
document.getElementById();

document.getElementsByClassName();

document.getElementsByTagName();

document.querySelector(); // Give the first element. Argument is like CSS Selector

document.querySelectorAll(); // Give the NodeList of elements. Argument is like CSS Selector

// innerText  give the text only

//innerHTML  give the everything inside the selected element

// TextContent  give the all text inside the selected element even if it is hidden
